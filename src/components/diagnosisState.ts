export function getDiagnosisSelectionMessage(label: string): string {
  return `Diagnosis locked in: ${label}. The debrief will score whether it matched the case and whether your plan supported it.`;
}
